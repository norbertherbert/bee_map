# Bee Map Backend

```bash

# make sure that you are in the backend folder of the repo (bee_map/backend)

# install the dependencies
npm install

# create your own .env file based on the .env.example file

# start the http server
npm start

```
